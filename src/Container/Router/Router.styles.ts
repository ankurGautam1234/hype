import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
});

export const background = {
    linearGradient: {
      colors: ['#000000', '#0B264F'],
      start: [0, 0],
      end: [1, 1]
    }
}

export default styles;